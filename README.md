Fire prediction.py and Fire prediction_knn.py were the python files used to run the Random Forest and KNN models. 
Fire_map.py turns the output csv of the models into a fire_map of probabilities
The remaining files were used for data conversion and handlling. These are not all the files used in this process,
but these are the ones I could find.
